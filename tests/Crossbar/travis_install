#!/bin/sh
set -x

sudo useradd -u 1000 crossbar # crossbar requires non-root docker - so we create a 'crossbar' user

mkdir crossbar

cd crossbar

git clone https://github.com/crossbario/crossbar-starter.git
